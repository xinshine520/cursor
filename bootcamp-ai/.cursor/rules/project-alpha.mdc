---
alwaysApply: true
---
# Project Alpha 项目规则

## 项目概述

Project Alpha 是一个基于标签分类的 Ticket 管理系统，采用前后端分离架构。

## 项目结构

```
project-alpha/
├── backend/          # FastAPI 后端服务
│   ├── app/          # 应用代码
│   │   ├── api/      # API 路由
│   │   ├── models/   # SQLAlchemy 模型
│   │   ├── schemas/  # Pydantic 模式
│   │   ├── crud/     # 数据库操作
│   │   └── services/ # 业务逻辑
│   ├── alembic/      # 数据库迁移
│   └── tests/        # 测试文件
└── frontend/         # React + Vite 前端应用
    └── src/
        ├── components/  # React 组件
        ├── pages/      # 页面组件
        ├── services/   # API 服务
        ├── hooks/      # 自定义 Hooks
        └── types/      # TypeScript 类型定义
```

## 技术栈

### 后端
- **Python**: 3.12+
- **框架**: FastAPI 0.123.x
- **数据库**: PostgreSQL 18.x
- **ORM**: SQLAlchemy 2.0 (async)
- **迁移工具**: Alembic
- **包管理**: uv
- **代码格式化**: black (line-length=100), isort
- **类型检查**: mypy (strict mode)

### 前端
- **框架**: React 19.x
- **构建工具**: Vite 7.x
- **语言**: TypeScript 5.x
- **样式**: Tailwind CSS 4.x + Less
- **UI 组件库**: Ant Design 5.x
- **状态管理**: React Query (@tanstack/react-query)
- **路由**: React Router DOM 7.x
- **HTTP 客户端**: Axios

## 代码规范

### Python 后端规范

1. **类型提示**
   - 所有函数必须包含类型提示
   - 使用 Python 3.12+ 的类型特性（如 `str | None`）

2. **异步编程**
   - 所有数据库操作必须使用 `async/await`
   - 使用 SQLAlchemy 2.0 的异步模式

3. **API 设计**
   - 使用 FastAPI 的依赖注入系统
   - 所有路由必须定义响应模型
   - 使用 Pydantic v2 进行数据验证

4. **错误处理**
   - 使用 FastAPI 的 `HTTPException`
   - 自定义异常继承 `HTTPException`

5. **代码格式**
   - 使用 black 格式化（line-length=100）
   - 使用 isort 排序导入
   - 使用 mypy 进行类型检查（strict mode）

### TypeScript 前端规范

1. **组件规范**
   - 使用函数组件和 Hooks
   - Props 必须定义接口类型
   - 使用 TypeScript 严格模式

2. **文件组织**
   - 组件文件：`ComponentName.tsx`
   - 样式文件：`ComponentName.less`
   - 类型文件：`types.ts`
   - 服务文件：`service.ts`

3. **样式规范**
   - 优先使用 Tailwind CSS 工具类
   - 复杂样式使用 Less 文件
   - 避免内联样式（除非动态样式）

4. **状态管理**
   - 组件状态使用 `useState`
   - 服务器状态使用 React Query
   - 全局状态使用 Context API

5. **API 调用**
   - 使用 Axios 进行 HTTP 请求
   - 所有 API 调用封装在 `services/` 目录
   - 使用 React Query 管理数据获取和缓存

## 命名约定

### Python 后端
- **文件**: 小写字母，下划线分隔（`user_service.py`）
- **类**: 大驼峰（`UserService`）
- **函数**: 小写字母，下划线分隔（`get_user_by_id`）
- **常量**: 全大写，下划线分隔（`MAX_RETRY_COUNT`）

### TypeScript 前端
- **文件**: 大驼峰（`UserProfile.tsx`）
- **组件**: 大驼峰（`UserProfile`）
- **函数**: 小驼峰（`getUserById`）
- **常量**: 全大写，下划线分隔（`API_BASE_URL`）
- **类型/接口**: 大驼峰（`UserProfileProps`）

## Git 提交规范

使用 Conventional Commits 格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**:
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具配置
- `perf`: 性能优化

**示例**:
```
feat(ticket): add search functionality
fix(api): resolve pagination issue
docs(readme): update installation guide
```

## 数据库规范

1. **迁移管理**
   - 所有数据库变更必须通过 Alembic 迁移
   - 迁移文件命名：`{revision}_{description}.py`
   - 迁移必须可逆（包含 upgrade 和 downgrade）

2. **模型设计**
   - 所有模型继承 `Base`
   - 使用 SQLAlchemy 2.0 语法
   - 必须定义类型提示

3. **关系定义**
   - 使用 `relationship()` 定义关系
   - 明确指定 `back_populates` 或 `backref`

## API 设计规范

1. **URL 设计**
   - 使用 RESTful 风格
   - 版本前缀：`/api/v1/`
   - 资源使用复数形式：`/api/v1/tickets/`

2. **响应格式**
   - 成功响应：返回数据对象
   - 错误响应：使用标准错误格式
   - 分页响应：包含 `items`、`total`、`skip`、`limit`

3. **状态码**
   - `200`: 成功获取/更新
   - `201`: 成功创建
   - `204`: 成功删除（无内容）
   - `400`: 请求错误
   - `404`: 资源不存在
   - `500`: 服务器错误

## 测试规范

### 后端测试
- 使用 pytest 和 pytest-asyncio
- 测试文件命名：`test_*.py`
- 测试函数命名：`test_*`
- 使用 `httpx.AsyncClient` 测试 API

### 前端测试
- 使用 Vitest（如果配置）
- 测试文件命名：`*.test.tsx` 或 `*.spec.tsx`
- 组件测试使用 React Testing Library

## 环境变量

### 后端环境变量
- `DATABASE_URL`: 数据库连接字符串
- `SECRET_KEY`: 密钥（如果使用）
- `DEBUG`: 调试模式

### 前端环境变量
- `VITE_API_BASE_URL`: API 基础 URL
- `VITE_APP_TITLE`: 应用标题

## 开发工作流

1. **创建功能分支**
   ```bash
   git checkout -b feature/ticket-search
   ```

2. **开发代码**
   - 编写代码和测试
   - 运行格式化工具
   - 运行类型检查

3. **提交代码**
   ```bash
   git commit -m "feat(ticket): add search functionality"
   ```

4. **推送和合并**
   - 推送到远程仓库
   - 创建 Pull Request
   - 代码审查后合并

## 禁止事项

- ❌ 不要直接修改数据库（使用 Alembic 迁移）
- ❌ 不要在生产代码中使用 `print()`（使用日志）
- ❌ 不要忽略类型错误
- ❌ 不要提交 `.env` 文件
- ❌ 不要使用同步数据库操作（使用 async）
- ❌ 不要在前端硬编码 API URL（使用环境变量）
