# Cursor AI 规则配置

## 通用规则

### 会话总结
每一次会话请求结束后进行会话总结，无论生成新文件还是修改已有文件都需要做总结，
并将总结内容Append写入到Readme文件中(说明文件中的内容是累积增加的)。
总结内容应包括：
   - 会话的主要目的
   - 完成的主要任务
   - 关键决策和解决方案
   - 使用的技术栈
   - 修改了哪些文件

### 语言和响应
- 始终使用中文简体进行交流和代码注释
- 代码注释使用中文，保持清晰简洁
- 变量名和函数名使用英文，遵循命名规范

### 代码风格
- 遵循 PEP 8 规范（Python 项目）
- 使用 4 个空格缩进，不使用 Tab
- 每行代码长度不超过 100 个字符
- 函数和类之间使用两个空行分隔
- 导入语句按标准库、第三方库、本地模块的顺序组织

### 代码质量
- 优先使用类型提示（Type Hints）
- 编写清晰的文档字符串（Docstrings）
- 避免使用魔法数字，使用有意义的常量
- 函数保持单一职责，避免过长函数（建议不超过 50 行）
- 优先使用列表推导式、生成器等 Pythonic 写法

### 错误处理
- 使用具体的异常类型，避免裸露的 `except:`
- 提供有意义的错误消息
- 记录异常信息，便于调试

### 文件组织
- 每个模块保持单一职责
- 使用 `__init__.py` 明确包的公共接口
- 配置文件使用 `.env` 或 `config.json`，不要硬编码敏感信息
- 测试文件使用 `test_` 前缀或放在 `tests/` 目录

### 文档和注释
- 为所有公共函数、类和方法编写文档字符串
- 使用 Google 风格或 NumPy 风格的文档字符串
- 复杂逻辑必须添加注释说明
- 保持 README.md 文件更新，记录项目变更

### Git 和版本控制
- 提交信息使用中文，格式：`类型: 简短描述`
  - 类型包括：feat（新功能）、fix（修复）、docs（文档）、style（格式）、refactor（重构）、test（测试）、chore（构建/工具）
- 提交前检查代码格式和基本错误
- 不要提交敏感信息（API 密钥、密码等）

### 依赖管理
- 使用 `requirements.txt` 或 `pyproject.toml` 管理依赖
- 明确指定版本号，避免使用 `*` 或 `latest`
- 定期更新依赖，注意安全漏洞

### 性能优化
- 避免过早优化，优先保证代码可读性
- 使用适当的数据结构和算法
- 注意内存使用，避免内存泄漏
- 对性能关键代码进行性能分析

### 安全性
- 不要硬编码密钥、密码等敏感信息
- 使用环境变量或配置文件管理敏感信息
- 验证和清理用户输入
- 使用参数化查询防止 SQL 注入（如适用）

### 测试
- 编写单元测试覆盖核心功能
- 测试函数使用 `test_` 前缀
- 使用断言（assert）验证预期结果
- 保持测试代码简洁清晰

### 代码审查
- 提交前进行自我审查
- 检查代码是否符合项目规范
- 确保没有调试代码或注释掉的代码
- 检查是否有未使用的导入或变量

## 项目特定规则

### 文件修改
- 修改文件前先读取现有内容，理解上下文
- 保持代码风格一致性
- 修改后检查是否有语法错误或 lint 错误

### 新功能开发
- 先理解需求，再开始编码
- 设计清晰的接口和数据结构
- 考虑向后兼容性
- 更新相关文档

### 问题解决
- 遇到错误时，先分析错误信息
- 查看相关文档和示例
- 提供清晰的错误处理和用户提示
- 记录解决方案，便于后续参考

### 会话总结
- 每次会话结束后，总结本次会话的主要内容
- 记录完成的任务、关键决策和技术选择
- 更新 README.md 文件，累积记录项目变更历史
- 总结内容包括：
  - 会话的主要目的
  - 完成的主要任务
  - 关键决策和解决方案
  - 使用的技术栈
  - 修改了哪些文件

## 响应格式

### 代码展示
- 使用代码块展示代码
- 对于现有代码，使用代码引用格式（包含文件路径和行号）
- 对于新代码，使用标准 markdown 代码块

### 解释说明
- 使用清晰的结构化说明
- 重要信息使用加粗或列表突出
- 提供代码示例说明用法

### 错误处理
- 遇到错误时，提供清晰的错误说明
- 给出可能的解决方案
- 必要时提供相关文档链接

